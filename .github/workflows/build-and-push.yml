name: Deploy-Dev

on:
  push:
    branches: [main]

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}
  REPO_NAME: ${{ github.repository }}
  USER_NAME: ${{ github.actor }}

jobs:
  build:
    runs-on: ubuntu-20.04
    permissions:
      contents: read
      packages: write
      id-token: write

    steps:
      # Send LINE notification at the beginning
      - name: Send LINE notification
        run: |
          curl -X POST https://notify-api.line.me/api/notify \
          -H 'Authorization: Bearer ${{ secrets.LINE_NOTIFY_TOKEN }}' \
          -d 'message=🚀 *เริ่มการทำงานของแอคชั่น!* 🎉
          🔹 *ที่เก็บข้อมูล:* ${{ env.REPO_NAME }} 
          🔹 *ผู้ใช้:* ${{ env.USER_NAME }} 
          👍 *ติดตามผลการทำงานต่อไป...*'

      - name: Checkout repository
        uses: actions/checkout@v3

      # Setup Docker buildx
      - name: Setup Docker buildx
        uses: docker/setup-buildx-action@v2

      # Login to GitHub Container Registry
      - name: Log into GitHub Container Registry
        uses: docker/login-action@v2
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      # Extract metadata (tags, labels) for Docker
      - name: Extract Docker metadata
        id: meta
        uses: docker/metadata-action@v4
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          tags: |
            type=ref,event=branch

      # Build and push Docker image
      - name: Build and push Docker image
        uses: docker/build-push-action@v4
        with:
          context: .
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}

      # Send LINE notification at the end
      - name: Send LINE notification
        run: |
          curl -X POST https://notify-api.line.me/api/notify \
          -H 'Authorization: Bearer ${{ secrets.LINE_NOTIFY_TOKEN }}' \
          -d 'message=🚀 *แอคชั่นเสร็จสมบูรณ์!* 🎉
          🔹 *รีโพ:* ${{ env.REPO_NAME }}
          🔹 *ผู้ใช้:* ${{ env.USER_NAME }}\n🔹 *อิมเมจ:* ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          🔹 *แท็ก:* ${{ steps.meta.outputs.tags }}
          👍 *ขอบคุณที่ให้ความสนใจ!*'
